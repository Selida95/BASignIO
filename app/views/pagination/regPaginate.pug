footer
	nav(aria-label='Page navigation')

		ul.pagination
			if pageNum == 1
				li.disabled
					span(aria-hidden='true')
						span.glyphicon.glyphicon-step-backward(aria-hidden="true")
				li.disabled
					span(aria-hidden='true')
						span.glyphicon.glyphicon-chevron-left(aria-hidden="true")
			else
				li
					a(href='/users/' + user.username + '/registers?page=1' + search, aria-label='Previous')
						span.glyphicon.glyphicon-step-backward(aria-hidden="true")
				li
					a(href='/users/' + user.username + '/registers?page=' + prevPage + search aria-label='Previous')
						span.glyphicon.glyphicon-chevron-left(aria-hidden="true")


			- for (var x = fvp; x <= lvp ; x++)

				if x == pageNum
					li.active
						a(href='/users/' + user.username + '/registers?page=' + x + search)= x
				else
					li
						a(href='/users/' + user.username + '/registers?page=' + x + search)= x


			if pageNum == totalPages
				li.disabled
					span(aria-hidden='true')
						span.glyphicon.glyphicon-chevron-right(aria-hidden="true")
				li.disabled
					span(aria-hidden='true')
						span.glyphicon.glyphicon-step-forward(aria-hidden="true")
			else
				if totalPages == 0
					li.disabled
						span(aria-hidden='true')
							span.glyphicon.glyphicon-chevron-right(aria-hidden="true")
					li.disabled
						span(aria-hidden='true')
							span.glyphicon.glyphicon-step-forward(aria-hidden="true")

				else
					li
						a(href='/users/' + user.username + '/registers?page=' + nextPage + search, aria-label='Next')
							span.glyphicon.glyphicon-chevron-right(aria-hidden="true")
					li
						a(href='/users/' + user.username + '/registers?page=' + totalPages + search, aria-label='Next')
							span.glyphicon.glyphicon-step-forward(aria-hidden="true")

			| Total # of pages: #{totalPages}